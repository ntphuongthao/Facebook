<%= form_with(url: search_path, method: :get, local: true) do |f| %>
  <%= f.text_field :query, placeholder: "Search your friends here..." %>
  <%= button_tag type: 'submit', class: 'search-button' do %>
    Submit
  <% end %>
<% end %>